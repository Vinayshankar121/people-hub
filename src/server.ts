// Server entry for TanStack Start — re-exports the default handler.
// Nitro's vercel preset (configured in vite.config.ts) wraps this
// into a Vercel serverless function automatically.
export { default } from "@tanstack/react-start/server-entry";
